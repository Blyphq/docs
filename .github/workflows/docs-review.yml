# Generated by @farming-labs/docs. You can edit this file.
name: Docs Review

on:
  pull_request:
    paths:
      - "docs.config.tsx"
      - "docs/**"
      - "app/docs/**"
      - "src/app/docs/**"
      - "content/docs/**"
      - "src/content/docs/**"
      - "src/lib/docs.config.*"
      - ".github/workflows/docs-review.yml"
      - ".github/workflows/docs-review-reusable.yml"

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  docs-review:
    name: "docs-review"
    uses: farming-labs/docs/.github/workflows/docs-review-reusable.yml@main
    with:
      check-name: "docs-review"
      config: "docs.config.tsx"
      working-directory: "."
      package-manager: "pnpm"
      pnpm-version: "10"
    permissions:
      contents: read
      checks: write
      pull-requests: write
