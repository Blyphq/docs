# Blyp Docs

Documentation site for [Blyp](https://github.com/Blyphq/blyp), built with `@farming-labs/docs` on Next.js.

## Run locally

```bash
pnpm dev
```

## Build

```bash
pnpm build
```

## Docs entry

The docs content lives under `app/docs/`. The site root redirects to `/docs`.
